export interface BlogPreviewProps {
  imageDisplay: boolean;
  textDisplay: boolean;
  row: boolean;
  wide: boolean;
  border: boolean;
  link: string;
  headline: string;
  text: string;
  imageUrl: string;
}
